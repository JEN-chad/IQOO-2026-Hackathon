# SafeScreen AI — model export/eval deps.
#
# IMPORTANT: pin ExecuTorch to the SAME version as the Android runtime AAR
# (org.pytorch:executorch-android:0.6.0) so exported .pte files load on-device.
# A newer ExecuTorch (e.g. 1.3.x) writes a .pte schema the 0.6.0 runtime can't open.
#
# Create an ISOLATED env for this (don't reuse a base env that has a newer executorch):
#   python3 -m venv tools/.venv && source tools/.venv/bin/activate
#   pip install -r tools/requirements.txt
#
# If pip reports a torch version conflict, let executorch pick torch:
#   pip install "executorch==0.6.0" && pip install timm transformers torchvision pillow numpy

executorch==0.6.0
torch==2.6.0
torchvision==0.21.0
timm==1.0.11
transformers==4.46.0
pillow
numpy
